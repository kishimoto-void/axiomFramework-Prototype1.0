# Integration tests

End-to-end checks for:

```
PSS → PLP → Capsule → ACP → DCK → Difference Report
```

Add tests here only after Phase 4 Runtime wire-up.
Prefer Golden locks over brittle string snapshots of full reports.
